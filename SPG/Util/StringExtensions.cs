using System;
using System.Collections.Generic;
using System.Text;

namespace SPG.Util
{
    public static class StringExtensions
    {
        //public static long GetUniqueHashCode(this string s)
        //{
        //    var bytes = Encoding.ASCII.GetBytes(s);
        //    string numerical = "";            
        //    foreach (var c in bytes)
        //    {
        //        numerical += $"{c}";
        //    }

        //    return long.Parse(numerical);
        //}
    }
}
